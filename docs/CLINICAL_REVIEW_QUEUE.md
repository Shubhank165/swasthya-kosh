# Clinical review queue

Everything in `clinical/` that an engineer authored and a clinician has not yet
confirmed. This is the agenda for the AIIA mentor session — a list of questions
about clinical content, not a list of known defects.

Two things worth stating before the session:

- **Nothing here is a diagnostic rule.** This system records what a patient
  reports and what their documents say. It never states a diagnosis, never gives
  advice, and never decides anything about a patient's care. The interaction
  table below flags a pair for a physician to look at; it does not recommend
  stopping anything.
- **Every item can be changed by editing `clinical/**` alone.** None of it needs
  a code change. That is the point of keeping the content out of Python, and it
  means a Vaidya can review and correct these in a pull request.

Compiled by hand on 2026-09-05. The previous version was generated by
`scripts/review_queue.py`, which read the pathway and red-flag YAML that now
lives in `backend/stale/` — the interview runs on the Jetson and this repository
no longer holds those files.

---

## 1. The Hindi report wording — 36 strings, 14 sections

`clinical/report_templates/hi/report.yaml`

**The highest-value item on this list.** The Hindi file is written as Hindi
rather than translated from the English at runtime, and the two are allowed to
differ where clinical Hindi differs from clinical English. It was written by an
engineer working from reference material, which is not the same as written by a
clinician who speaks to patients in Hindi every day.

Read as a whole, out loud, by somebody who does. The specific questions:

- [ ] Does `not_established` read as *"यह जानकारी प्राप्त नहीं हुई"* — the
      information was not obtained — and never as a negative? A line that reads
      as "the patient does not have this" where the truth is "nobody asked" is
      the single worst failure this document can have.
- [ ] Are `unresolved`, `not_asked`, `not_applicable` and `refused` four
      visibly different statements in Hindi? They are four different
      medico-legal positions and the English file keeps them apart; the Hindi
      must too.
- [ ] Is the header disclaimer — that this is patient-reported, unverified, and
      contains no diagnosis — phrased the way a physician would expect to see it?
- [ ] Are the section headings the words used in an AYUSH OPD, or the words a
      dictionary gives?

The English file (`en/report.yaml`, same 36 strings) has had eyes on it and is
the reference for meaning, not for phrasing.

## 2. The drug–drug interaction table — 20 rules

`clinical/interactions/seed.yaml`

Every row carries a source and a row without one does not load. The sources are
the WHO Model Formulary 2008 monographs and the CDSCO *National Formulary of
India* 6th ed. What has **not** been reviewed is the *selection*: whether these
twenty are the right twenty for an Indian AYUSH OPD population, and whether any
severity is pitched wrongly.

**Scope, deliberately:** drug–drug only, among medicines the patient is already
taking. **There is no herb–drug prediction and none should be added without
evidence** — this is exactly where a plausible false positive would have a
physician stop a patient's Ayurvedic medicine on the authority of a kiosk.

- [ ] `atenolol` + `verapamil` — major: additive effect on heart rate and cardiac conduction  
      source: WHO Model Formulary 2008, verapamil monograph
- [ ] `enalapril` + `potassium_chloride` — major: additive risk of raised serum potassium  
      source: WHO Model Formulary 2008, ACE inhibitor monograph
- [ ] `enalapril` + `spironolactone` — major: additive risk of raised serum potassium  
      source: WHO Model Formulary 2008, ACE inhibitor monograph
- [ ] `ibuprofen` + `warfarin` — major: additive bleeding risk  
      source: WHO Model Formulary 2008, NSAID monograph
- [ ] `metformin` + `contrast_media` — major: reported risk of lactic acidosis around iodinated contrast administration  
      source: WHO Model Formulary 2008, metformin monograph
- [ ] `methotrexate` + `trimethoprim` — major: additive antifolate effect  
      source: WHO Model Formulary 2008, methotrexate monograph
- [ ] `rifampicin` + `oral_contraceptive` — major: reported reduction in contraceptive efficacy  
      source: WHO Model Formulary 2008, rifampicin monograph
- [ ] `warfarin` + `aspirin` — major: additive bleeding risk  
      source: CDSCO National Formulary of India, 6th ed., Appendix: drug interactions
- [ ] `warfarin` + `ciprofloxacin` — major: reported increase in anticoagulant effect  
      source: WHO Model Formulary 2008, ciprofloxacin monograph
- [ ] `warfarin` + `fluconazole` — major: reported increase in anticoagulant effect  
      source: WHO Model Formulary 2008, fluconazole monograph
- [ ] `amitriptyline` + `tramadol` — moderate: additive serotonergic effect and lowered seizure threshold  
      source: WHO Model Formulary 2008, tramadol monograph
- [ ] `amlodipine` + `simvastatin` — moderate: reported increase in simvastatin exposure  
      source: WHO Model Formulary 2008, simvastatin monograph
- [ ] `clopidogrel` + `omeprazole` — moderate: reported reduction in clopidogrel activation  
      source: WHO Model Formulary 2008, clopidogrel monograph
- [ ] `digoxin` + `furosemide` — moderate: diuretic-induced low potassium may increase digoxin toxicity  
      source: WHO Model Formulary 2008, digoxin monograph
- [ ] `glibenclamide` + `ciprofloxacin` — moderate: reported disturbance of blood glucose control  
      source: WHO Model Formulary 2008, sulfonylurea monograph
- [ ] `ibuprofen` + `enalapril` — moderate: NSAIDs may reduce the antihypertensive effect and affect renal function  
      source: WHO Model Formulary 2008, NSAID monograph
- [ ] `levothyroxine` + `calcium_carbonate` — moderate: reported reduction in levothyroxine absorption when taken together  
      source: WHO Model Formulary 2008, levothyroxine monograph
- [ ] `levothyroxine` + `ferrous_sulphate` — moderate: reported reduction in levothyroxine absorption when taken together  
      source: WHO Model Formulary 2008, levothyroxine monograph
- [ ] `metronidazole` + `alcohol` — moderate: reported disulfiram-like reaction  
      source: WHO Model Formulary 2008, metronidazole monograph
- [ ] `phenytoin` + `isoniazid` — moderate: reported increase in phenytoin concentration  
      source: WHO Model Formulary 2008, phenytoin monograph

- [ ] Is any of these twenty not worth flagging in this setting?
- [ ] Is any common pair in an Indian OPD missing?
- [ ] Is any `severity` wrong? `major` is rendered more prominently than
      `moderate`.

## 3. Ingredient aliases — 32 entries

`clinical/interactions/ingredients.yaml`

Maps what is written on a prescription to the ingredient the interaction table
is keyed on: brand names, dosage forms, spellings. Deliberately a list rather
than a fuzzy matcher, because an interaction flag raised on a spelling guess is
worse than no flag.

- [ ] Are the brand names the ones actually written in Delhi OPD prescriptions?
- [ ] Any common brand missing? A missing alias means an interaction is silently
      not flagged, which is the failure direction that matters.

## 4. Concepts flagged in the registry — 4 of 103

`clinical/terminology/concepts.yaml`

Each of these carries `needs_clinical_review: true` in the file itself. Wording
and clinical meaning need confirmation:

- [ ] `abdominal_rigidity`
- [ ] `thunderclap_onset`
- [ ] `menstrual_history`
- [ ] `ritu_seasonal_variation` — seasonal variation of symptoms (Ritu). The one
      Ayurveda-specific concept on this list, and the one most likely to be
      worded wrongly by a non-Vaidya.

The other 99 concepts are ordinary history-taking vocabulary and are not flagged.

## 5. Terminology seeds — development placeholders

| File | Contents | Status |
|---|---|---|
| `namaste_seed.yaml` | 20 concepts | `needs_clinical_review: true` |
| `icd11_tm2_seed.yaml` | 6 concepts | `needs_clinical_review: true` |
| `icd11_mms_seed.yaml` | 10 concepts | biomedical, for dual coding |
| `conceptmap.yaml` | 8 mappings | `needs_clinical_review: true` |

**These are curated seeds shaped like the real releases, not the releases.** They
exist so the system runs end-to-end offline in CI and at a demo without shipping
licensed content into a public repository. The loader in
`app/services/terminology.py` ingests a full NAMASTE release into the same
tables.

- [ ] **They must be replaced with the licensed releases before any clinical
      use.** This is a licensing and procurement task, not a review task.
- [ ] Of the 8 mappings, are any wrong? Where no mapping exists there is no
      entry, and nothing in the codebase may synthesise one — an invented
      mapping is worse than an absent one because it looks authoritative.

## 6. The consent notice — 1 version, 4 purposes

`clinical/consent/consent_v1.yaml`

`clinical_source` records it as *"drafted against DPDP Act 2023 notice
requirements; pending institutional legal review"*.

- [ ] **Institutional legal review**, not clinical review. The DPDP Act requires
      that we can produce, for any patient, exactly what they agreed to, in
      which language, when, and who granted it — which is why the text is
      versioned content rather than a string in a template.
- [ ] Is the Hindi notice text right? Same concern as item 1, with legal weight.
- [ ] `raw_audio_retention` is a separately granted purpose and is off unless
      the patient grants it. Confirm that separation is what the institution
      wants.

---

## Not on this list

The pathways, the red-flag rules and the coverage model. They were reviewed
items in the previous build and they are no longer this service's concern: the
interview, question selection and red-flag evaluation run on the Jetson at the
kiosk. This backend receives already-fired red flags as events and persists
them; it does not evaluate rules. See `backend/stale/README.md`.
