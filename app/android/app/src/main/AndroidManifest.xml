<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app posts the intake to the hospital. Declared here rather than
         only in the debug manifest, where the Flutter tool puts it for hot
         reload: without it in `main`, a release build cannot reach the network
         at all and every submission fails on the one build a patient installs. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- RECORD_AUDIO is for on-device voice input only — the mic feeds an
         offline recogniser (sherpa_onnx) that runs on the phone, and the audio
         is never written to disk or sent anywhere. See the DECISIONS entry that
         reversed the touch-only stance and `test/on_device_voice_test.dart`,
         which asserts no cloud speech package sits beside it and nothing in
         lib/voice/ reaches the network. CAPTURE_AUDIO_OUTPUT and
         MODIFY_AUDIO_SETTINGS remain forbidden. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- `usesCleartextTraffic="false"` below: health answers never travel in
         plaintext. That is already the platform default above API 27, but a
         default is something a future targetSdk change can move, so it is
         stated. The debug manifest overrides it for a laptop backend on a LAN,
         and only there. -->

    <application
        android:label="medikiosk_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
